/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { EditTarotFormComponent } from './editTarotForm.component';

describe('EditTarotFormComponent', () => {
  let component: EditTarotFormComponent;
  let fixture: ComponentFixture<EditTarotFormComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ EditTarotFormComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(EditTarotFormComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/